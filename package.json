{
  "name": "a11y-multilevel-menu",
  "version": "1.0.0",
  "description": "A responsive accessible menu with screen reader and keyboard support. ",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx tailwindcss -i ./assets/source/styles.css -o ./assets/css/styles.css --watch --postcss",
    "build": "npx tailwindcss -i ./assets/source/styles.css -o ./assets/css/styles.min.css --minify --postcss"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.27",
    "postcss-import": "^15.1.0",
    "postcss-nested": "^6.0.1",
    "tailwindcss": "^3.3.7"
  }
}
